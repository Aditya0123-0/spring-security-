spring:
  application:
    name: security-jpa
  port: 8081

  datasource:
    url: jdbc:postgresql://localhost:5432/user
    username: postgres
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true